{
  "command": {
    "publish": {
      "conventionalCommits": true,
      "createRelease": "github",
      "exact": true,
      "ignoreChanges": ["**/*.md", "**/*.spec.*", "**/*.test.*"],
      "message": "chore(release): publish",
      "yes": true
    },
    "version": {
      "allowBranch": "master"
    }
  },
  "hoist": true,
  "nohoist": "react*",
  "packages": ["packages/*"],
  "version": "independent"
}
